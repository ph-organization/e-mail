<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--namespace mapper接口 必填 随便填值 防止SQL语句ID重名-->
<mapper namespace="com.puhui.email.mapper.RoleMapper">

    <!--    //根据邮箱查询自己的角色-->
    <select id="roleSelect" parameterType="string" resultType="com.puhui.email.entity.Role">
        SELECT ro.* FROM t_role ro,mail_user mu,t_realtion rea WHERE ro.id=rea.role_id and mu.id=rea.user_id and mu.email=#{email};
    </select>
    <!--根据角色查询-->
    <select id="roleSelectNote" parameterType="string" resultType="com.puhui.email.entity.Role">
        SELECT * FROM t_role WHERE  role_note=#{role_note};
    </select>
    <!--根据角色查询该角色下所有用户-->
    <select id="roleSelectUser" parameterType="string" resultType="com.puhui.email.entity.MailUser">
        SELECT mu.* FROM t_realtion res ,mail_user mu,t_role ro WHERE  res.user_id=mu.id and res.role_id=ro.id and ro.role_note=#{role_note};
    </select>
    <!--根据用户邮箱查询该用户拥有的角色集合（通过角色组查询）-->
    <select id="roleSelectByEmail" parameterType="string" resultType="com.puhui.email.entity.Role">
        SELECT ro.* FROM t_role ro,mail_user mu,role_rolegroup rrg,mailuser_rolegroup mrg,role_group rg WHERE ro.id=rrg.role_id and rrg.role_group_id=rg.id and rg.id=mrg.role_group_id and mrg.mailuser_id=mu.id and mu.email=#{email}
    </select>
    <!--根据角色名查询角色下的所有邮件用户（通过角色组查询）-->
    <select id="mailUserSelectByRoleName" parameterType="string" resultType="com.puhui.email.entity.MailUser">
        SELECT mu.* FROM t_role ro,mail_user mu,role_rolegroup rrg,mailuser_rolegroup mrg,role_group rg WHERE ro.id=rrg.role_id and rrg.role_group_id=rg.id and rg.id=mrg.role_group_id and mrg.mailuser_id=mu.id  and ro.role_note=#{role_note}
    </select>
    <!--根据角色id查询角色下的所有邮件用户（通过角色组查询）-->
    <select id="mailUserSelectByRoleId" parameterType="integer" resultType="com.puhui.email.entity.MailUser">
        SELECT mu.* FROM t_role ro,mail_user mu,role_rolegroup rrg,mailuser_rolegroup mrg,role_group rg WHERE ro.id=rrg.role_id and rrg.role_group_id=rg.id and rg.id=mrg.role_group_id and mrg.mailuser_id=mu.id  and ro.id=#{id}
    </select>
    <!--根据角色组组名查询（通过用户组查询）主要拿到id-->
    <select id="roleGroupSelectByRoleGroupName" parameterType="string" resultType="com.puhui.email.entity.RoleGroup">
        SELECT * FROM role_group WHERE group_name=#{group_name}
    </select>
</mapper>